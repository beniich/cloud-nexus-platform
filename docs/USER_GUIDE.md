# 🌍 Cloud Nexus - Guide Utilisateur Multilingue

Ce guide explique comment utiliser les fonctionnalités multilingues de la plateforme Cloud Nexus.

## 1. Changer de Langue

La plateforme supporte actuellement les langues suivantes :
- **Français (FR)** - Langue par défaut
- **Anglais (EN)**
- **Arabe (AR)** - Avec support RTL (Right-to-Left)

### Comment changer la langue ?
1. Localisez le sélecteur de langue dans la **barre de navigation** (en haut à droite).
2. Cliquez sur l'icône de globe ou le drapeau actuel.
3. Sélectionnez la langue désirée dans le menu déroulant.

Le changement est **instantané** et votre préférence est **sauvegardée** pour vos prochaines visites (via le stockage local du navigateur).

---

## 2. Support RTL (Arabe)

Lorsque vous sélectionnez l'Arabe (العربية), l'interface s'adapte automatiquement :
- L'affichage passe en mode **miroir** (droite vers gauche).
- Les menus, alignements de texte et icônes directionnelles s'inversent.
- Cette disposition est native et ne nécessite aucun rechargement de page.

## 3. Gestion des Erreurs et Messages

Les messages d'erreur et de succès (par exemple lors de la connexion ou d'une demande de service) sont automatiquement traduits dans la langue choisie.

---

# 🌍 Cloud Nexus - Multilingual User Guide

This guide explains how to use the multilingual features of the Cloud Nexus platform.

## 1. Changing Language

The platform currently supports the following languages:
- **French (FR)** - Default
- **English (EN)**
- **Arabic (AR)** - With RTL (Right-to-Left) support

### How to switch language?
1. Locate the language switcher in the **navbar** (top right).
2. Click on the globe icon or current flag.
3. Select your desired language from the dropdown menu.

The change is **instant** and your preference is **saved** for future visits (via browser local storage).

## 2. RTL Support (Arabic)

When you select Arabic (العربية), the interface automatically adapts:
- The layout switches to **mirror** mode (Right-to-Left).
- Menus, text alignment, and directional icons are flipped.
- This layout change is native and requires no page reload.

## 3. Error Handling & Messages

Error and success messages (e.g., during login or service request) are automatically translated into the selected language.
